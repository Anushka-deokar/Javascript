//String methods

// Methods : actions that can be performed on objects
// Format : string_name.method()

let msg = " Hello ";
msg.trim();
console.log(msg);
//trims whitespaces from both ends of string and returns a new string

// strings are immutable in JS
// whenver we try to use any method it returns new string

let name = "Anushka";

console.log(name.toLowerCase());

console.log(name.toUpperCase());